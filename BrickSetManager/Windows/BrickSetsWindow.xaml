<Window x:Class="BrickSetManager.Windows.BrickSetsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sets Containing This Brick" Height="500" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <StackPanel>
                <TextBlock x:Name="BrickNameTextBlock" FontSize="18" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="BrickNumberTextBlock" FontSize="14" Foreground="#BDC3C7" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Sets list -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <DataGrid x:Name="SetsDataGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowHeight="60" Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Set Number" Binding="{Binding SetNumber}" Width="120"/>

                    <DataGridTextColumn Header="Quantity in Set" Binding="{Binding Quantity}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</Window>
